# Manager-session handoff — 2026-08-02

Read CLAUDE.md first (constitution + rules 0/0b/15/15b). This file is the
exact pickup point for the next Manager session.

## Where we are (all committed & pushed on master)

- **Estimator = rulings 29-33** (docs/DECISIONS.md), ONE implementation in
  hockeycore/fit/prior_fit.py: calendar recency (12-mo half-life, fitted),
  predictive per-league prior strength, whisper-past-3 anchor fade, and
  ruling 33 — 75%+ raw record on >=2 chances caps the league anchor at 1
  chance-equivalent (Seb's examples are exact code behavior: weight 3/5 and
  1/1, cap 3/4 and 2/2). Refit strengths: NHL 3, AHL 9, Liiga 4.
- **Rule 28 amended: betting floor 40%** (was 50) on the BASE coach %.
  Fav/dog effect lives in card Notes only. Hot-form badge (28b) removed
  from card display; still logged by paper harness.
- **Morning cards** (morning_bot.py): compact 6-line block per team —
  Pull % (career k/n), 2025-26 P/NP, last 5 clean chances (2-season
  window), recency-weighted avg pull time (2 seasons), Notes. Sample in
  Desktop\HOCKEYPROJECTS\_manager\morning_cards_sample.md.
- **Pull reports**: hockeycore/io/build_pull_reports.py (rule 13 — now
  scripted) -> 3 xlsx in _manager, rebuilt on ruling-33 numbers.
- Reference points Seb has challenged (current values): Söderholm TPS 3/3
  -> 84%, Quenneville 3/3 -> 87%, Parent UTC 8/10 -> 78%, Potvin TUC
  14/17 -> 85%, McIlvane SD 19/25 -> 84%.

## Validation status at handoff: ALL GREEN under ruling 33

17/17 gates passed (incl. the NHL ROI gate P(>edge)>0.94 on the fresh
walk-forward rows). Liiga blind: lead1 +0.018, total1 -0.002, total2
-0.010, marg4 +0.057, bad 1/10 — inside every gate bound; ROI@10%EV
+17.1/+11.9/+28.5. AHL blind still fails (leaderTT -4.0%, -3.5 -20.8%)
exactly as ruling 24 pins — NO-GO stands, nothing new. backtest_rows*.json
in data/derived/ are the ruling-33 refresh, committed. Per rule 0 the next
session still re-runs the gate suite before relying on any of this.

## Queue (in priority order)

1. Gates re-run (above).
2. Playoff-race covariate: needs standings-as-of-date builder from lakes
   (Seb doctrine: stage-of-season + race matter; calendar stage tested
   NULL within-coach, race untested).
3. Group-conditional fav/dog: sub-50% pullers showed +11pts (z=2.6,
   diluted) — confirm on clean chances only before wiring into cards.
4. Special notes system: data/coach_maps/special_notes.csv (team,note)
   feeds card Notes — in-depth team studies to populate it.
5. September: automation package (nightly fetchers + odds on Seb's PC,
   Task Scheduler), paper-harness shakedown (AHL GameStatus/clock, Liiga
   live clock field, AHL 26-27 season id), settlement join script.
6. October: paper-trade month. AHL re-litigation ~Dec (needs 150-200
   fresh 26-27 instances). AHL -3.5 permanently no-bet (ruling 24).
   -3.5 all leagues stays CAUTION (double-edge-or-skip).

## Working protocol with Seb (hard-learned)

- Answer with numbers IMMEDIATELY; run validation silently in background;
  interrupt only if a gate goes red. Always END turns with visible text.
- Deliverables: SendUserFile + device_commit_files to
  C:\Users\seb_1\OneDrive\Desktop\HOCKEYPROJECTS\_manager\ (force=true).
- Seb's PC: C:\dev\HOCKEYPROJECTS_V2 is NOT a git clone (fetchers live in
  Desktop\HOCKEYPROJECTS\_manager\v2_bootstrap; paste-block PowerShell
  works when file downloads don't).
- Long jobs: setsid nohup + marker files; bash tool timeout is 10 min.
- Commit every change in-session (stop-hook enforces it).
